import '../../../../../utils/common_imports.dart';

class WaveChartPage extends StatelessWidget {
  const WaveChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wave Chart Page'),
      ),
    );
  }
}
